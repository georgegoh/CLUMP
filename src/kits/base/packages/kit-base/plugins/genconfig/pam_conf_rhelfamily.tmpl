#
# Dynamically generated by: genconfig  (Do not edit!)"
#
## namespace required: None
#
# Note that this configuration file is meant for rhel/centos nodes only.

auth        required      pam_env.so
auth        sufficient    pam_unix2.so
auth        requisite     pam_succeed_if.so uid >= 10000 quiet
auth        required      pam_deny.so

account     required      pam_unix2.so
account     sufficient    pam_succeed_if.so uid < 500 quiet
account     required      pam_permit.so

password    requisite     pam_cracklib.so try_first_pass retry=3
password    sufficient    pam_unix2.so nullok use_authtok
password    required      pam_deny.so

session     optional      pam_keyinit.so revoke
session     required      pam_limits.so
session     [success=1 default=ignore] pam_succeed_if.so service in crond quiet use_uid
session     required      pam_unix2.so
